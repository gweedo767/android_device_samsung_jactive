# system.prop for jactive
#

# cannot take spaces
rild.libargs=-d /dev/smd0


telephony.lteOnGsmDevice=1
ro.telephony.default_network=9

# reboot mode for bootloader option in recovery
ro.bootloader.mode=download

